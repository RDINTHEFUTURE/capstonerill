# Plan: Hapus Fitur Trial Balance

## Goal
Hapus fitur Trial Balance (`/reports/trial-balance`) karena fungsinya sama dengan General Ledger (`/ledger`) dan Ledger lebih lengkap.

## Changes Required

### 1. Hapus file view
- **Hapus** `resources/views/reports/trial-balance.blade.php`

### 2. Hapus routes (`routes/web.php`)
Hapus 2 baris route:
- Line 70: `Route::get('reports/export-trial-balance', ...)`
- Line 71: `Route::get('reports/trial-balance', ...)`

### 3. Hapus methods di controller (`app/Http/Controllers/ReportController.php`)
Hapus 2 methods:
- `exportTrialBalance()` (lines 147-189)
- `trialBalance()` (lines 354-381)

### 4. Update sidebar (`resources/views/partials/sidebar.blade.php`)
- Line 83 & 89: Hapus `|| request()->routeIs('reports.trial-balance')` dari kondisi active/open
- Lines 102-107: Hapus seluruh `<li>` menu item "Neraca Saldo"

### 5. Fix ledger view (`resources/views/ledger/index.blade.php`)
- Line 8: Ganti `route('reports.export-trial-balance')` → `route('reports.export-ledger')`

### 6. Update documentation (`DOCUMENTATION.md`)
Hapus referensi trial-balance di lines 562-563, 684-686, 826.

## Verification
- Buka sidebar, menu "Neraca Saldo" harus hilang dari submenu Accounting
- Route `/reports/trial-balance` harus return 404
- `/ledger` tetap berfungsi normal dengan tombol Export CSV yang mengarah ke export-ledger
- Tidak ada error di Laravel log
