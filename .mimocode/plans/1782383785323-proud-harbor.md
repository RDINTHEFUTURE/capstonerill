# Plan: Remove Unused Code

## Files to Delete

### Unused CSS (not linked in any blade template)
- `public/css/invoice.css` — classes duplicated in `invoice-preview.css` and `invoice-pdf.css`
- `public/css/invoice-cetakfaktur.css` — never linked; `cetakfaktur.blade.php` uses inline styles instead

### Empty Blade Templates (no route or controller references them)
- `resources/views/invoices/surat_jalan.blade.php` — 0 lines
- `resources/views/invoices/purchase_order.blade.php` — 0 lines
- `resources/views/invoices/invoice_supplier.blade.php` — 0 lines
- `resources/views/invoices/faktur_pajak.blade.php` — 0 lines

### Unused View (no route maps to it)
- `resources/views/welcome.blade.php` — Laravel default scaffold, never referenced in `web.php` or any controller

### Empty/Unused Model
- `app/Models/InvoiceAdminSupplier.php` — empty class, no migration exists, never imported anywhere

## Verification
- Run `php artisan route:list` to confirm no broken route references
- Run `npm run build` (if applicable) to check for missing asset references
