# Fix: Set Timezone to WIB (Asia/Jakarta)

## Problem
Timezone is set to UTC, showing wrong times.

## Fix
Change `'timezone' => 'UTC'` to `'timezone' => 'Asia/Jakarta'` in `config/app.php`.

## File to modify
- `config/app.php` (line 68)
