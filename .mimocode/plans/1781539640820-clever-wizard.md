# Fix: Authentication Protection

## Problem
The `/` (home) route is not protected by auth middleware. Unauthenticated users can access the dashboard.

## Fix
Move the `/` route inside the `auth` middleware group in `routes/web.php`.

## File to modify
- `routes/web.php`
