# Plan: Code Comment Optimization

## Goal
Add high-value comments to files where they explain business logic, security constraints, concurrency patterns, and non-obvious decisions. Remove redundant/obvious comments.

## Files to Comment

### 1. `app/Services/JournalService.php` (Highest priority)
**Why:** Double-entry accounting logic is domain-critical and non-obvious.

Comments to add:
- `postInvoice()`: Explain the business rule — every credit sale creates exactly two balanced entries (Debit Piutang, Credit Penjualan). Note that this method is idempotent (reverses before posting).
- `reverseInvoice()`: Explain that this exists because `postInvoice()` is called on both create and update — it prevents duplicate entries.
- `getBalance()`: Explain that balance = debit - credit, and that `COALESCE` handles accounts with no entries.

**Existing comments to review:**
- `// Handle QR upload (DJP provided image)` — Valuable (DJP = Indonesian tax authority)
- `// Fallback: generate QR that links to the PDF (legacy behavior)` — Valuable
- `// Simpan di database sebagai base64(JSON)...` — Valuable, but translate to English

### 2. `app/Services/InvoiceNumberService.php` (High priority)
**Why:** Race condition prevention is non-obvious and critical.

Comments to add:
- `generate()`: Explain that `lockForUpdate()` prevents two concurrent requests from generating the same invoice number. Without this lock, two simultaneous invoice creations could get the same number, violating the unique constraint.

### 3. `app/Http/Controllers/InvoiceController.php` (High priority)
**Why:** Complex validation, business rules, and security patterns.

Comments to add:
- Lines 99-114: Explain why signature validation is conditional — QR requires a DJP-provided image file, hand requires base64-drawn signature data. These are mutually exclusive paths.
- Lines 118-142: Explain the subtotal formula: `(harga * qty) - diskon`, and why `max(0, ...)` is applied — prevents negative subtotals when discount exceeds item value.
- `duplicate()`: Explain that the duplicate resets signature and status — the duplicated invoice needs fresh signature and starts as unpaid.
- `qr()` method: Explain the three-tier fallback: (1) return stored DJP QR image if uploaded, (2) generate QR linking to PDF if no image exists, (3) regenerate payload if stale.

**Existing comments to review:**
- `// Enforce required fields based on selected signature type` — Redundant (code is self-explanatory)
- `// refresh items` — Remove (obvious)
- `// handle QR upload (DJP provided image)` — Valuable
- `// Fallback: generate QR that links to the PDF (legacy behavior)` — Valuable
- `// Simpan di database sebagai base64(JSON)...` — Translate to English

### 4. `app/Http/Controllers/UserController.php` (Medium priority)
**Why:** Role hierarchy is a security-critical business rule.

Comments to add:
- `authorizeUser()`: Explain that only Admin, Manager, and Supervisor can manage users. Staff cannot create/edit other accounts.
- `canManageUser()`: Explain the hierarchical rule — you can only manage users with a lower role level. You cannot manage yourself or peers.
- Lines 62-76 and 156-170: Explain the role escalation prevention — each role can only assign roles strictly below their own level.

### 5. `app/Models/Invoice.php` (Medium priority)
**Why:** Audit trail and soft deletes are architectural decisions.

Comments to add:
- `SoftDeletes`: Explain that soft deletes are used to preserve audit trail and allow potential recovery of accidentally deleted invoices.
- `created_by`: Explain this is an audit field — it tracks which user created the invoice and is immutable after creation.
- `signature_type`: Explain that this field was missing from the database schema initially and required a migration fix (2026_06_19_000001).

### 6. `app/Http/Controllers/ReportController.php` (Low priority)
**Why:** Authorization and streaming patterns.

Comments to add:
- `authorizeReport()`: Explain that export/import is restricted to Admin, Manager, and Supervisor — Staff should not bulk-download financial data.
- Export methods: Explain the streaming pattern — `response()->stream()` sends CSV data progressively without buffering the entire file in memory.

### 7. `app/Http/Controllers/InvoicePdfController.php` (Low priority)
- Explain `isRemoteEnabled` — allows DomPDF to load external resources (CDN CSS/JS) in the PDF template.

## Comments to Remove
- `// Hand signature` (InvoiceController line 62) — Empty section header, remove
- `// refresh items` (InvoiceController line 314) — Obvious from the code
- Empty `// Seller` comment blocks in validation arrays — redundant with field names

## Verification
Run `php artisan test` — all 7 tests must still pass after adding comments.
